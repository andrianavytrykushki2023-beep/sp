Program _Р;
Var 
  Int16 _A, _B, _I, _J, _X;

Begin
  Get(_A);
  Get(_B);

  For _I -> _A To _B Do
    Put(_I Mul _I);

  _X -> 0;

  For _I -> 1 To _A Do
    For _J -> 1 To _B Do
      _X -> _X + 1;

  Put(_X);
End
